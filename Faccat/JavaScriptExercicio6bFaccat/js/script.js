/* Escreva um algoritmo para ler as dimensões de um quadrado (base x altura), calcular e escrever a área do retânguo. */

alert("Quadrado Área");

let base = parseFloat(prompt("Digite a base: "));
let altura = parseFloat(prompt("Digite a altura: "));

let areaQuadrado = base * altura;

alert("A base desse Quadrado é: " + areaQuadrado);