/* Escreva um algoritmo para ler as dimensões de um trapézio ((base de cima + base de baixo) * )altura / 2)), calcular e escrever a área do retânguo. */

alert("Trapézio Área");

let baseSuperior = parseFloat(prompt("Digite a base superior: "));

let baseInferior = parseFloat(prompt("Digite a base inferior: "));

let altura = parseFloat(prompt("Digite a altura: "));

let areaTrapezio = (baseSuperior + baseInferior) * (altura / 2);

alert("A área desse trapézio é: " + areaRetangulo);